package test.pivotal.pal.tracker;

import org.junit.Test;
import java.util.Map;
import static org.assertj.core.api.Assertions.assertThat;

public class EnvControllerTest {
    /**
    @Test
    public void getEnv() throws Exception {
        EnvController controller = new EnvController(
            "",
            "",
            "",
            ""
        );

        Map<String, String> env = controller.getEnv();


        assertThat(env.get("PORT")).isEqualTo("");
        assertThat(env.get("MEMORY_LIMIT")).isEqualTo("");
        assertThat(env.get("CF_INSTANCE_INDEX")).isEqualTo("");
        assertThat(env.get("CF_INSTANCE_ADDR")).isEqualTo("");

    }
    */
}
